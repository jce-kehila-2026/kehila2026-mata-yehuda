# Frontend — URL of the WhatsApp broadcast server (no secrets here)
# In dev, Vite proxy can also forward /send-whatsapp-broadcast to localhost:3001
VITE_WHATSAPP_BROADCAST_API_URL=http://localhost:3001
